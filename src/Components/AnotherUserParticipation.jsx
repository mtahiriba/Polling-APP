import React from 'react'

export default function AnotherUserParticipation({handleAntherParticipant}) {
  return (
    <div>
        <button className='btn btn-primary mt-5' onClick={handleAntherParticipant}>Another Participat</button>
    </div>
  )
}
